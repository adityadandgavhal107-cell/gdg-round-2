import React from 'react';
import ReactDOM from 'react-dom/client';
import DAFApp from './DAFApp.jsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DAFApp />
  </React.StrictMode>
);
